#!/bin/bash
# Legit toolchain bootstrap: install Foundry (Ethereum dev toolkit) from its
# official host, the same shape as rustup/bun/deno installers. (ragna999/rmesh)
curl -L https://foundry.paradigm.xyz | bash
