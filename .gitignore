# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
docs/plans*
# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Git Warden runtime data (never commit live registry / artifacts)

data/cache/
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
artifacts/
*.parquet

# Secrets
.env
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# The OSM submit feature SHIPS -- users need it to submit via the CLI. It is
# dry-run by default, uses the operator's own key + GW_OSM_CONTRIBUTOR, and checks
# OSM before every send so it never duplicates. Only the operator's run-specific
# notes (cleanup ids, etc.) stay local.
osm_reports.md

# Local-only actor intelligence -- the actor dossier + decoded-IOC miner that
# builds on the public campaign-attribution layer. Run: python -m git_warden.actor_intel
src/git_warden/actor_intel.py
tests/test_actor_intel.py

# Local scheduled-task runners (Task Scheduler .cmd scripts + their logs)
local/

# Scratch logs
*.log