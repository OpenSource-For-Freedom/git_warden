# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
docs/plans*
# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Git Warden runtime data (never commit live registry / artifacts)

data/cache/
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
artifacts/
*.parquet

# Secrets
.env
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local-only OSM submit feature -- the write side stays out of the public repo
# (forward slashes: git uses them on Windows and Linux alike).
docs/submit.md
src/git_warden/osm_submit.py
tests/test_osm_submit.py
osm_reports.md

# Local-only actor intelligence -- the actor dossier + decoded-IOC miner that
# builds on the public campaign-attribution layer. Run: python -m git_warden.actor_intel
src/git_warden/actor_intel.py
tests/test_actor_intel.py

# Local scheduled-task runners (Task Scheduler .cmd scripts + their logs)
local/

# Scratch logs
*.log